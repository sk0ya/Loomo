<Window x:Class="sk0ya.Loomo.App.Views.WorkspaceEditPreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loomo - 編集プレビュー"
        Width="960" Height="680" MinWidth="640" MinHeight="420"
        ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Bg}" FontFamily="Segoe UI">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="SummaryText" Grid.Row="0"
                   Foreground="{DynamicResource Fg}" FontSize="{DynamicResource Fs14}"
                   TextWrapping="Wrap" Margin="0,0,0,6" />
        <TextBlock x:Name="OperationText" Grid.Row="1"
                   Foreground="{DynamicResource FgDim}" FontSize="{DynamicResource Fs11}"
                   TextWrapping="Wrap" Margin="0,0,0,10" />
        <TextBox x:Name="DiffText" Grid.Row="2"
                 IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap"
                 HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                 FontFamily="Cascadia Mono, Consolas" FontSize="12"
                 Background="{DynamicResource BgAlt}" Foreground="{DynamicResource Fg}"
                 BorderBrush="{DynamicResource Border}" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="適用" IsDefault="True" MinWidth="90"
                    Style="{StaticResource FlatButton}" Click="OnApply" />
            <Button Content="キャンセル" IsCancel="True" MinWidth="100" Margin="8,0,0,0"
                    Style="{StaticResource FlatButton}" Background="{DynamicResource BgAlt}"
                    Foreground="{DynamicResource Fg}" />
        </StackPanel>
    </Grid>
</Window>
