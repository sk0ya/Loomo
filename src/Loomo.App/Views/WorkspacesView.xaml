<UserControl x:Class="sk0ya.Loomo.App.Views.WorkspacesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource BgAlt}">

    <DockPanel>
        <Grid DockPanel.Dock="Top">
            <TextBlock Style="{StaticResource PanelHeader}" Text="WORKSPACES" />
            <Button HorizontalAlignment="Right" Margin="0,4,6,0"
                    Width="30" Height="26"
                    Style="{StaticResource CaptionButton}"
                    ToolTip="フォルダを選択"
                    Command="{Binding OpenFolderCommand}">
                <Path Width="15" Height="13" Stretch="Uniform"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                      StrokeThickness="1.6"
                      Data="M1,3.5 H5 L6.2,5 H14 V12 H1 Z M1,3.5 V2 H5.5 L6.7,3.5" />
            </Button>
        </Grid>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="8,0,8,10">
            <ItemsControl ItemsSource="{Binding Workspaces}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="WorkspaceBorder" Margin="0,2"
                                Background="Transparent"
                                BorderBrush="{StaticResource Border}"
                                BorderThickness="1"
                                CornerRadius="4">
                            <Button Padding="8,6"
                                    HorizontalContentAlignment="Stretch"
                                    Foreground="{StaticResource Fg}"
                                    Command="{Binding DataContext.ActivateWorkspaceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                              VerticalAlignment="Center" />
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding RootPath}"
                                               Foreground="{StaticResource FgDim}"
                                               FontSize="11"
                                               Margin="0,2,0,0"
                                               TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </Button>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                <Setter TargetName="WorkspaceBorder" Property="Background" Value="{StaticResource Bg}" />
                                <Setter TargetName="WorkspaceBorder" Property="BorderBrush" Value="{StaticResource Accent}" />
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
