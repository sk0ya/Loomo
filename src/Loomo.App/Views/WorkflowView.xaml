<UserControl x:Class="sk0ya.Loomo.App.Views.WorkflowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:sk0ya.Loomo.App.ViewModels"
    Background="{DynamicResource BgAlt}">

    <UserControl.Resources>
        <!-- 前段参照チップ（クリックで指示文へ {{N}} を挿入） -->
        <Style x:Key="RefChipButton" TargetType="Button">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Margin" Value="0,0,4,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{DynamicResource BgAlt}" CornerRadius="9"
                                BorderBrush="{DynamicResource Border}" BorderThickness="1" Padding="7,1">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{DynamicResource Bg}" />
                                <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource Accent}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ステップ見出しの種別セグメント（テキスト/エージェント）。ModeSegButton より小ぶり。 -->
        <Style x:Key="StepTypeSeg" TargetType="Button" BasedOn="{StaticResource ModeSegButton}">
            <Setter Property="Padding" Value="0" />
            <Setter Property="MinWidth" Value="0" />
        </Style>

        <!-- カードのホバーで現れる小さな操作アイコン（↑↓＋🗑） -->
        <Style x:Key="StepActionButton" TargetType="Button" BasedOn="{StaticResource IconButton}">
            <Setter Property="Width" Value="24" />
            <Setter Property="Height" Value="24" />
            <Setter Property="FontSize" Value="12" />
        </Style>

        <!-- ツールバーの控えめなボタン（開く/保存/新規） -->
        <Style x:Key="ToolbarGhostButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
            <Setter Property="Padding" Value="9,4" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="4,0,0,0" />
        </Style>

        <!-- 開くポップアップの保存済みワークフロー行 -->
        <Style x:Key="SavedRowButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{DynamicResource BgAlt}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />   <!-- ツールバー -->
            <RowDefinition Height="Auto" />   <!-- 実行ステータスバー -->
            <RowDefinition Height="*" />      <!-- ステップのパイプライン -->
        </Grid.RowDefinitions>

        <!-- ===== ツールバー：名前・開く・保存・新規・実行 ===== -->
        <Border Grid.Row="0" BorderBrush="{DynamicResource Border}" BorderThickness="0,0,0,1" Padding="10,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- ワークフロー名：枠なしの見出し風。空のときはプレースホルダを下敷きに出す。 -->
                <Grid Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">
                    <TextBlock Text="ワークフロー名" IsHitTestVisible="False" VerticalAlignment="Center"
                               FontSize="15" Foreground="{DynamicResource FgDim}"
                               Visibility="{Binding Name, Converter={StaticResource EmptyToVis}}" />
                    <TextBox x:Name="NameBox" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                             Background="Transparent" BorderThickness="0" Padding="0"
                             FontSize="15" FontWeight="SemiBold" VerticalContentAlignment="Center"
                             Foreground="{DynamicResource Fg}" CaretBrush="{DynamicResource Fg}"
                             ToolTip="ワークフロー名" />
                </Grid>

                <!-- 開く：保存済みワークフローをポップアップ一覧から選ぶ -->
                <ToggleButton x:Name="OpenToggle" Grid.Column="1" Content="📂 開く ▾"
                              Foreground="{DynamicResource Fg}" Background="Transparent"
                              BorderBrush="{DynamicResource Border}" BorderThickness="1"
                              Padding="9,4" FontSize="12" Cursor="Hand"
                              ToolTip="保存済みワークフローを開く">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="4"
                                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource BgAlt}" />
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource BgAlt}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
                <Popup PlacementTarget="{Binding ElementName=OpenToggle}" Placement="Bottom"
                       StaysOpen="False" AllowsTransparency="True"
                       IsOpen="{Binding IsChecked, ElementName=OpenToggle}">
                    <Border Background="{DynamicResource Panel}" BorderBrush="{DynamicResource Border}"
                            BorderThickness="1" CornerRadius="6" Padding="4" MinWidth="240" MaxWidth="360"
                            Margin="0,2,8,8">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.3" />
                        </Border.Effect>
                        <Grid>
                            <TextBlock Text="保存済みのワークフローはありません" Margin="8,6" FontSize="12"
                                       Foreground="{DynamicResource FgDim}" TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SavedWorkflows.Count}" Value="0">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <ItemsControl ItemsSource="{Binding SavedWorkflows}" MaxHeight="320">
                                <ItemsControl.Template>
                                    <ControlTemplate TargetType="ItemsControl">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                                            <ItemsPresenter />
                                        </ScrollViewer>
                                    </ControlTemplate>
                                </ItemsControl.Template>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Button Grid.Column="0" Style="{StaticResource SavedRowButton}"
                                                    Click="OnSavedWorkflowClick"
                                                    Command="{Binding DataContext.LoadWorkflowCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}">
                                                <TextBlock Text="{Binding Name}" Foreground="{DynamicResource Fg}"
                                                           FontSize="12" TextTrimming="CharacterEllipsis" />
                                            </Button>
                                            <Button Grid.Column="1" Style="{StaticResource StepActionButton}" Content="🗑"
                                                    ToolTip="このワークフローを削除"
                                                    Command="{Binding DataContext.DeleteWorkflowCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                </Popup>

                <Button Grid.Column="2" Content="保存" Style="{StaticResource ToolbarGhostButton}"
                        Command="{Binding SaveCommand}" ToolTip="このワークフローを保存／上書き" />
                <Button Grid.Column="3" Content="新規" Style="{StaticResource ToolbarGhostButton}"
                        Command="{Binding NewWorkflowCommand}" ToolTip="新しいワークフローを作る" />

                <Border Grid.Column="4" Width="1" Margin="10,2" Background="{DynamicResource Border}" />

                <!-- 実行／停止：唯一のアクセント主操作 -->
                <Button Grid.Column="5" Content="▶ 実行" Style="{StaticResource FlatButton}"
                        Command="{Binding RunCommand}" ToolTip="上から順にステップを実行"
                        Visibility="{Binding IsRunning, Converter={StaticResource InverseBoolToVis}}" />
                <Button Grid.Column="5" Content="⏹ 停止" Style="{StaticResource FlatButton}"
                        Background="#FF6E1E1E" Command="{Binding CancelCommand}"
                        Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}" />
            </Grid>
        </Border>

        <!-- ===== 実行ステータスバー（実行中＝進捗／停止後＝結果メッセージ） ===== -->
        <Border Grid.Row="1" Background="{DynamicResource BgAlt}" Padding="12,5"
                BorderBrush="{DynamicResource Border}" BorderThickness="0,0,0,1">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding RunStatus}" Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel Orientation="Horizontal">
                <ProgressBar IsIndeterminate="True" Width="80" Height="4"
                             Foreground="{DynamicResource Accent}" Background="Transparent"
                             BorderThickness="0" VerticalAlignment="Center"
                             Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}" />
                <TextBlock Text="{Binding RunStatus}" VerticalAlignment="Center" FontSize="12"
                           Margin="0,0,0,0" Foreground="{DynamicResource FgDim}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                    <Setter Property="Margin" Value="10,0,0,0" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- ===== ステップのパイプライン ===== -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="10,8,10,12">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding Steps}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:WorkflowStepViewModel}">
                            <!-- 行ルート（ホバーで操作アイコンを出す）。縦の隙間を作らず接続線を連続させる。 -->
                            <Grid x:Name="RowRoot">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="30" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- 左レール：上下の接続線＋番号バッジ（状態で色分け） -->
                                <Grid Grid.Column="0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="0" Width="2" HorizontalAlignment="Center"
                                            Background="{DynamicResource Border}"
                                            Visibility="{Binding IsFirst, Converter={StaticResource InverseBoolToVis}}" />
                                    <Border Grid.Row="1" Width="24" Height="24" CornerRadius="12"
                                            HorizontalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{DynamicResource BgAlt}" />
                                                <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
                                                <Setter Property="BorderThickness" Value="1.5" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Running">
                                                        <Setter Property="Background" Value="{DynamicResource Accent}" />
                                                        <Setter Property="BorderThickness" Value="0" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Done">
                                                        <Setter Property="Background" Value="#FF4CAF50" />
                                                        <Setter Property="BorderThickness" Value="0" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Error">
                                                        <Setter Property="Background" Value="#FFE05252" />
                                                        <Setter Property="BorderThickness" Value="0" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Index}" FontSize="11" FontWeight="SemiBold"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource FgDim}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Running">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Done">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Error">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                    <Border Grid.Row="2" Width="2" HorizontalAlignment="Center"
                                            Background="{DynamicResource Border}"
                                            Visibility="{Binding IsLast, Converter={StaticResource InverseBoolToVis}}" />
                                </Grid>

                                <!-- ステップカード -->
                                <Border Grid.Column="1" Margin="2,5,2,5" CornerRadius="8" Padding="10"
                                        Background="{DynamicResource Bg}"
                                        BorderBrush="{DynamicResource Border}" BorderThickness="1">
                                    <StackPanel>
                                        <!-- 見出し行：種別セグメント・タイトル・状態・操作 -->
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <!-- テキスト / エージェント 切替 -->
                                            <Border Grid.Column="0" Background="{DynamicResource BgAlt}"
                                                    BorderBrush="{DynamicResource Border}" BorderThickness="1"
                                                    CornerRadius="5" Padding="2" VerticalAlignment="Center">
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Content="テキスト" Style="{StaticResource StepTypeSeg}"
                                                            Padding="8,3" Command="{Binding SelectModeCommand}"
                                                            CommandParameter="text"
                                                            Tag="{Binding IsTextMode, Converter={StaticResource ActiveTagConverter}}"
                                                            ToolTip="ツールを使わない単発のテキスト応答" />
                                                    <Button Content="エージェント" Style="{StaticResource StepTypeSeg}"
                                                            Padding="8,3" Margin="2,0,0,0"
                                                            Command="{Binding SelectModeCommand}" CommandParameter="agent"
                                                            Tag="{Binding UseTools, Converter={StaticResource ActiveTagConverter}}"
                                                            ToolTip="ツール（コマンド/ファイル/検索）を使えるエージェント実行" />
                                                </StackPanel>
                                            </Border>

                                            <!-- タイトル（任意・枠なし） -->
                                            <Grid Grid.Column="1" Margin="8,0,8,0" VerticalAlignment="Center">
                                                <TextBlock Text="ステップ名（任意）" IsHitTestVisible="False"
                                                           VerticalAlignment="Center" FontSize="12"
                                                           Foreground="{DynamicResource FgDim}"
                                                           Visibility="{Binding Title, Converter={StaticResource EmptyToVis}}" />
                                                <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                                                         Background="Transparent" BorderThickness="0" Padding="0"
                                                         FontSize="12" FontWeight="SemiBold"
                                                         VerticalContentAlignment="Center"
                                                         Foreground="{DynamicResource Fg}" CaretBrush="{DynamicResource Fg}" />
                                            </Grid>

                                            <!-- 状態テキスト（待機／実行中…／完了 (2.1秒)／失敗） -->
                                            <TextBlock Grid.Column="2" Text="{Binding StatusText}" VerticalAlignment="Center"
                                                       Margin="0,0,6,0" FontSize="11" TextTrimming="CharacterEllipsis"
                                                       MaxWidth="160">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{DynamicResource FgDim}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Running">
                                                                <Setter Property="Foreground" Value="{DynamicResource Accent}" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Done">
                                                                <Setter Property="Foreground" Value="#FF4CAF50" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Error">
                                                                <Setter Property="Foreground" Value="#FFE05252" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>

                                            <!-- 操作（ホバーで出現：上へ/下へ/直後に挿入/削除） -->
                                            <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                                                <StackPanel.Style>
                                                    <Style TargetType="StackPanel">
                                                        <Setter Property="Opacity" Value="0" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsMouseOver, ElementName=RowRoot}" Value="True">
                                                                <Setter Property="Opacity" Value="1" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </StackPanel.Style>
                                                <Button Style="{StaticResource StepActionButton}" Content="↑" ToolTip="上へ移動"
                                                        Command="{Binding DataContext.MoveUpCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}" />
                                                <Button Style="{StaticResource StepActionButton}" Content="↓" ToolTip="下へ移動"
                                                        Command="{Binding DataContext.MoveDownCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}" />
                                                <Button Style="{StaticResource StepActionButton}" Content="＋" ToolTip="このすぐ下にステップを挿入"
                                                        Command="{Binding DataContext.InsertStepAfterCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}" />
                                                <Button Style="{StaticResource StepActionButton}" Content="🗑" ToolTip="このステップを削除"
                                                        Command="{Binding DataContext.RemoveStepCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}" />
                                            </StackPanel>
                                        </Grid>

                                        <!-- 指示文（プレースホルダ付き） -->
                                        <Grid Margin="0,8,0,0">
                                            <TextBlock IsHitTestVisible="False" Margin="6,5" FontSize="12"
                                                       Foreground="{DynamicResource FgDim}" TextWrapping="Wrap"
                                                       Text="AIへの指示を入力…   {{1}} などで前段の出力を差し込めます"
                                                       Visibility="{Binding Prompt, Converter={StaticResource EmptyToVis}}" />
                                            <TextBox Text="{Binding Prompt, UpdateSourceTrigger=PropertyChanged}"
                                                     Background="{DynamicResource BgAlt}"
                                                     BorderBrush="{DynamicResource Border}" BorderThickness="1"
                                                     Padding="6,5" AcceptsReturn="True" TextWrapping="Wrap"
                                                     MinHeight="48" MaxHeight="200" VerticalScrollBarVisibility="Auto"
                                                     Foreground="{DynamicResource Fg}" CaretBrush="{DynamicResource Fg}" />
                                        </Grid>

                                        <!-- 前段参照チップ -->
                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0"
                                                    Visibility="{Binding RefTokens.Count, Converter={StaticResource CountToVis}}">
                                            <TextBlock Text="参照:" VerticalAlignment="Center" Margin="0,0,6,4"
                                                       FontSize="11" Foreground="{DynamicResource FgDim}" />
                                            <ItemsControl ItemsSource="{Binding RefTokens}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Button Style="{StaticResource RefChipButton}"
                                                                Command="{Binding DataContext.InsertRefCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                CommandParameter="{Binding}"
                                                                ToolTip="クリックで指示文へ挿入">
                                                            <TextBlock Text="{Binding}" FontFamily="Cascadia Mono, Consolas"
                                                                       FontSize="11" Foreground="{DynamicResource Fg}" />
                                                        </Button>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>

                                        <!-- 実行ログの開閉（ログがあるときだけ） -->
                                        <Button HorizontalAlignment="Left" Margin="0,8,0,0" Padding="0"
                                                Background="Transparent" BorderThickness="0" Cursor="Hand"
                                                Command="{Binding ToggleLogCommand}"
                                                Visibility="{Binding Log.Count, Converter={StaticResource CountToVis}}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding LogGlyph}" FontSize="9" VerticalAlignment="Center"
                                                           Foreground="{DynamicResource FgDim}" Margin="0,0,5,0" />
                                                <TextBlock Text="実行ログ" FontSize="11" VerticalAlignment="Center"
                                                           Foreground="{DynamicResource FgDim}" />
                                            </StackPanel>
                                        </Button>

                                        <!-- 実行ログ（思考・ツールカード・承認・結果・出力） -->
                                        <ItemsControl ItemsSource="{Binding Log}" Margin="0,6,0,0"
                                                      Visibility="{Binding IsLogCollapsed, Converter={StaticResource InverseBoolToVis}}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate DataType="{x:Type vm:TranscriptEntry}">
                                                    <Border Margin="0,2" CornerRadius="4" Padding="6,4"
                                                            Background="{DynamicResource BgAlt}">
                                                        <StackPanel>
                                                            <TextBlock Text="{Binding Header}" FontSize="11"
                                                                       FontWeight="SemiBold" TextTrimming="CharacterEllipsis">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Foreground" Value="{DynamicResource FgDim}" />
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding Kind}" Value="Error">
                                                                                <Setter Property="Foreground" Value="#FFE05252" />
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Kind}" Value="Assistant">
                                                                                <Setter Property="Foreground" Value="{DynamicResource Fg}" />
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                            <TextBox Text="{Binding Text, Mode=OneWay}" IsReadOnly="True"
                                                                     TextWrapping="Wrap" BorderThickness="0" Padding="0"
                                                                     Margin="0,2,0,0" Background="Transparent"
                                                                     IsReadOnlyCaretVisible="False" IsTabStop="False" Cursor="IBeam"
                                                                     Foreground="{DynamicResource Fg}" FontSize="12">
                                                                <TextBox.Style>
                                                                    <Style TargetType="TextBox">
                                                                        <Setter Property="FontFamily" Value="Segoe UI" />
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding Kind}" Value="Tool">
                                                                                <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Kind}" Value="Thinking">
                                                                                <Setter Property="FontStyle" Value="Italic" />
                                                                                <Setter Property="Foreground" Value="{DynamicResource FgDim}" />
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Text}" Value="">
                                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBox.Style>
                                                            </TextBox>

                                                            <!-- 差分（編集系ツールの承認カード） -->
                                                            <Border Visibility="{Binding HasDiff, Converter={StaticResource BoolToVis}}"
                                                                    BorderBrush="{DynamicResource Border}" BorderThickness="1"
                                                                    CornerRadius="4" Margin="0,4,0,0" Padding="0,2">
                                                                <ItemsControl ItemsSource="{Binding DiffLines}">
                                                                    <ItemsControl.ItemTemplate>
                                                                        <DataTemplate>
                                                                            <Border Background="{Binding Background}">
                                                                                <TextBlock Text="{Binding Text}" Foreground="{Binding Foreground}"
                                                                                           FontFamily="Cascadia Mono, Consolas" FontSize="12"
                                                                                           Padding="6,0" />
                                                                            </Border>
                                                                        </DataTemplate>
                                                                    </ItemsControl.ItemTemplate>
                                                                </ItemsControl>
                                                            </Border>

                                                            <!-- 承認カード -->
                                                            <StackPanel Orientation="Horizontal" Margin="0,6,0,0"
                                                                        Visibility="{Binding IsPending, Converter={StaticResource BoolToVis}}">
                                                                <Button Content="承認して実行" Style="{StaticResource FlatButton}"
                                                                        Command="{Binding ApproveCommand}" Margin="0,0,8,0" />
                                                                <Button Content="拒否" Style="{StaticResource FlatButton}"
                                                                        Background="#FF6E1E1E" Command="{Binding RejectCommand}" />
                                                            </StackPanel>
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- 末尾の追加ノード：ライブラリから「ステップを追加」パレットを開く -->
                <Grid Margin="0,2,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="2" HorizontalAlignment="Center" Height="12"
                            VerticalAlignment="Top" Background="{DynamicResource Border}" />

                    <ToggleButton x:Name="AddPaletteToggle" Grid.Column="1" HorizontalAlignment="Left"
                                  Margin="2,4,0,0" Content="＋ ステップを追加 ▾"
                                  Foreground="{DynamicResource Fg}" Background="Transparent"
                                  BorderBrush="{DynamicResource Border}" Padding="11,5" FontSize="12"
                                  Cursor="Hand" ToolTip="小さなタスクの候補からステップを選んで追加">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="4"
                                        BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="b" Property="Background" Value="{DynamicResource BgAlt}" />
                                    </Trigger>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="b" Property="Background" Value="{DynamicResource BgAlt}" />
                                        <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource Accent}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                    </ToggleButton>

                    <!-- ステップ候補パレット（カテゴリ別）。行クリックで末尾に追加して閉じる。 -->
                    <Popup Grid.Column="1" PlacementTarget="{Binding ElementName=AddPaletteToggle}"
                           Placement="Bottom" StaysOpen="False" AllowsTransparency="True"
                           IsOpen="{Binding IsChecked, ElementName=AddPaletteToggle}">
                        <Border Background="{DynamicResource Panel}" BorderBrush="{DynamicResource Border}"
                                BorderThickness="1" CornerRadius="6" Padding="4" Width="300" Margin="0,2,8,8">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.3" />
                            </Border.Effect>
                            <StackPanel>
                                <Button Style="{StaticResource SavedRowButton}" Click="OnAddCandidateClick"
                                        HorizontalAlignment="Stretch"
                                        Command="{Binding DataContext.AddCandidateCommand, ElementName=AddPaletteToggle}"
                                        ToolTip="空のステップを追加して自分で書く">
                                    <TextBlock Text="＋ 空のステップ" FontSize="12" FontWeight="SemiBold"
                                               Foreground="{DynamicResource Fg}" />
                                </Button>
                                <Border Height="1" Background="{DynamicResource Border}" Margin="2,4" />
                                <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding DataContext.StepLibrary, ElementName=AddPaletteToggle}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type vm:StepCandidateGroup}">
                                                <StackPanel Margin="0,2,0,4">
                                                    <TextBlock Text="{Binding Category}" Margin="8,4,0,2"
                                                               FontSize="10.5" FontWeight="SemiBold"
                                                               Foreground="{DynamicResource FgDim}" />
                                                    <ItemsControl ItemsSource="{Binding Items}">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Button Style="{StaticResource SavedRowButton}"
                                                                        HorizontalAlignment="Stretch" Click="OnAddCandidateClick"
                                                                        Command="{Binding DataContext.AddCandidateCommand, ElementName=AddPaletteToggle}"
                                                                        CommandParameter="{Binding}"
                                                                        ToolTip="{Binding Prompt}">
                                                                    <StackPanel>
                                                                        <TextBlock Text="{Binding Name}" FontSize="12"
                                                                                   FontWeight="SemiBold" Foreground="{DynamicResource Fg}" />
                                                                        <TextBlock Text="{Binding Description}" FontSize="11"
                                                                                   Foreground="{DynamicResource FgDim}"
                                                                                   TextTrimming="CharacterEllipsis" />
                                                                    </StackPanel>
                                                                </Button>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </StackPanel>
                        </Border>
                    </Popup>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
