<Window x:Class="sk0ya.Loomo.App.Views.NewFileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新規ファイル"
        Width="440"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Bg}"
        FontFamily="Segoe UI">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="ファイル名と拡張子を指定してください。"
                   Foreground="{DynamicResource Fg}"
                   FontSize="{DynamicResource Fs13}"
                   Margin="0,0,0,10" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="112" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NameBox"
                     Grid.Column="0"
                     FontSize="{DynamicResource Fs13}"
                     ToolTip="例: MainWindow、または MainWindow.xaml" />
            <ComboBox x:Name="ExtensionBox"
                      Grid.Column="1"
                      Margin="8,0,0,0"
                      IsEditable="True"
                      FontSize="{DynamicResource Fs13}"
                      ToolTip="拡張子を選択または入力（例: .cs）">
                <ComboBoxItem Content="（なし）" />
                <ComboBoxItem Content=".md" />
                <ComboBoxItem Content=".pochi.json" />
                <ComboBoxItem Content=".flow" />
            </ComboBox>
        </Grid>

        <TextBlock Grid.Row="2"
                   Text="名前に拡張子が含まれている場合は、そちらをそのまま使います。"
                   Foreground="{DynamicResource FgDim}"
                   FontSize="{DynamicResource Fs11}"
                   TextWrapping="Wrap"
                   Margin="0,6,0,0" />

        <TextBlock x:Name="ErrorText"
                   Grid.Row="3"
                   Foreground="#FFE4676B"
                   FontSize="{DynamicResource Fs11}"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"
                   Margin="0,6,0,0" />

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="作成"
                    Style="{StaticResource FlatButton}"
                    IsDefault="True"
                    MinWidth="76"
                    Click="OnOk" />
            <Button Content="キャンセル"
                    Style="{StaticResource FlatButton}"
                    Background="{DynamicResource BgAlt}"
                    Foreground="{DynamicResource Fg}"
                    IsCancel="True"
                    MinWidth="92"
                    Margin="8,0,0,0" />
        </StackPanel>
    </Grid>
</Window>
