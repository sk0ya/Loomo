# Loomo の方向性アイデア

既存資産（Editor / Terminal / Browser / Git / Diff / Search / LSP / Workflow / Trace / AI tools）を「AI と人間が
一緒に作業する作業台」として束ねる方向のメモ。

## 候補

| 候補 | 内容（規模） |
|---|---|
| Transform Tray | Base64/URL エンコード・Unix 時間・Hash・GUID・JWT 解析・改行/インデント変換などを個別ペインにせず、入出力を持つ1つのトレイに集約。入力は Editor 選択範囲・Terminal 出力・Browser 選択・クリップボード、結果は元の場所または Pegboard へ返せる（小） |
| Encoding Inspector | 文字コード・BOM・改行・不可視文字・文字化け候補の解析（小） |
| Process Tree / Port Inspector | 親子プロセス・CPU・メモリ・起動引数。ポート⇔プロセス対応と開く/停止操作。ファイル使用中プロセスの特定（小〜中） |
| Verification Gate | 未保存・Git 状態・診断・build/test 結果をコミット前に1画面で確認（中） |
| Review Progress | Git 変更を確認済みとして記録し、未確認差分だけ巡回（中） |
| テストランナーの多言語化 | jest/vitest/pytest の口を開ける。現状は `dotnet test`＋TRX 決め打ち（中） |

## パーツ間の共通接続

新しいパーツは、可能な範囲で5つの共通操作を持つ: **ここへ送る**（Editor 選択・Terminal 出力・Browser 内容・
ファイルを渡す）／**元を開く**（結果から元ファイル・行・URL・Terminal タブへ戻る）／**比較する**（Diff の適切な
比較器へ）／**残す**（Pegboard へ）／**実行する**（見える Terminal で）。
共通の巨大なメッセージ基盤を先に作らず、最初のパーツに必要な最小の受け渡し型から始める。

## 上位概念：Semantic Depth

Loomo の主役をペインやファイルから、仕事とその意味的構造へ移す。Z 軸には「対象をどこまで深く理解しているか」
という**解像度だけ**を割り当て、時間は横方向、仮説は同深度の枝、事実／推測／検証状態は線種・透明度・色で表す
（一本の軸に複数の意味を載せると空間として理解できない）。航行できる深度は実データと Lens が揃った
**File / Symbol / Execution の3段**から始める。

> エラーからコードへ移動するのではなく、エラーへ潜ったら、その内側にコードがあった。

UI として成立させる制約:

- **中央を勝手に動かさない**: AI や新しいログは袖に候補として出し、ユーザー選択時のみ中心を移す。
- **一操作で一軸だけ動かす**: 深度移動・同深度移動・Lens 切替・時間移動を同時に行わない。
- **帰り道を常に示す**: `Esc` で一段、Breadcrumb で任意階層、履歴で直前の視点へ戻れる。
- **全情報を同時に出さない**: 全ノード・全時系列・全仮説・全 Lens・AI の全推論は俯瞰か一時展開に限定。
- **空の深度・Lens を見せない**: データの無い Lens は消さず、入手手段（テスト実行・デバッグアタッチ）を示す。
- **AI を移動の経路に入れない**: 候補列挙は LSP・Git・パーサなど決定論的情報源で即時に。ローカル LLM は
  非同期の後追い（注釈・仮説）に限定し、潜る／戻るの応答時間に関与させない。
