# AI Agent Studio 設計書

> アプリ名: **Loomo**（ルート名前空間: sk0ya.Loomo）
> 作成日: 2026-05-31

---

## 1. 概要

ローカルの開発ワークスペースを対象に動作する **AIエージェント搭載のデスクトップアプリ**（C# / WPF）。
ユーザーは自然言語で指示を出し、AIエージェントが **Terminal（コマンド実行）** と **Editor（ファイル表示・編集）** を操作しながらタスクを遂行する。

### コアコンセプト
- AIエージェントは UI を「人間と同じ道具」として使う。すなわち FolderTree で対象を把握し、Terminal で実行し、Editor で表示・編集する。
- UI の各ペインは **疎結合なサービス + メッセージング** で連携し、AIエージェントは「ツール（Tool）」を通じてそれらを駆動する。
- モダンな見た目（ダーク基調・フラット・VS Code 系の操作感）と、機能を後から足しやすい拡張構造を両立する。

---

## 2. 技術スタック

| 領域 | 採用 | 理由 |
|------|------|------|
| ランタイム | .NET 8 (LTS) もしくは .NET 9 | WPF 安定サポート。LTS優先なら .NET 8 |
| UIフレームワーク | WPF | 要件指定。リッチなデスクトップUIに最適 |
| アーキテクチャ | MVVM | テスト容易・View/ロジック分離 |
| MVVM基盤 | CommunityToolkit.Mvvm | `ObservableObject` / `RelayCommand` / `WeakReferenceMessenger` |
| DI / 起動 | Microsoft.Extensions.Hosting + DependencyInjection | サービス登録・ライフサイクル管理・設定・ロギングを統一 |
| ロギング | Microsoft.Extensions.Logging（+ Serilog 任意） | 構造化ログ |
| Terminal | **sk0ya.Terminal** (NuGet) | 要件指定 |
| Editor | **sk0ya.Editor** (NuGet) | 要件指定 |
| AIクライアント | Anthropic Claude API（Tool Use対応） | エージェントのツール実行に最適。`IAiClient` で抽象化し差し替え可能に |
| アイコン | 任意（Lucide / Fluent Icons の Path 化） | モダンな見た目 |

> AIプロバイダは `IAiClient` 抽象の背後に隠すため、Claude 以外（OpenAI等）への差し替えやローカルLLM対応も後から可能。

---

## 3. 全体アーキテクチャ

レイヤー構成（依存は上→下の一方向）:

```
┌─────────────────────────────────────────────────────────┐
│  Presentation (WPF)                                       │
│   Views (XAML) ── ViewModels ── Behaviors / Converters   │
└───────────────┬─────────────────────────────────────────┘
                │  Messenger（イベント） / DI
┌───────────────▼─────────────────────────────────────────┐
│  Application / Agent Core                                 │
│   AgentOrchestrator（エージェントループ）                 │
│   ToolRegistry / IAgentTool 群                            │
│   ApprovalService（実行承認）/ ConversationStore          │
└───────────────┬─────────────────────────────────────────┘
                │  サービス抽象（インターフェース）
┌───────────────▼─────────────────────────────────────────┐
│  Services / Adapters                                      │
│   ITerminalService → sk0ya.Terminal                      │
│   IEditorService   → sk0ya.Editor                        │
│   IWorkspaceService→ ファイルシステム / FolderTree状態     │
│   IAiClient        → Claude API                          │
└──────────────────────────────────────────────────────────┘
```

### 連携の要点
- **View 同士は直接参照しない。** すべて `WeakReferenceMessenger` のメッセージ、または共有サービス経由でやり取りする。
- AIエージェントは UI を直接触らず、**ツール → サービス → （メッセージ）→ View** という経路で操作する。これにより「AIが操作する」「人間が操作する」が同じ経路に集約され、テスト・記録・再現が容易になる。

---

## 4. UIレイアウト（確定案）

採用: **全幅の下部AIバー（展開式）**

```
┌────┬──────────┬───────────────┬────────────────┐
│    │          │               │                │
│ A  │  Folder  │   Terminal    │     Editor     │
│ c  │  Tree    │  (sk0ya.      │   (sk0ya.      │
│ t  │ (Sidebar)│   Terminal)   │    Editor)     │
│ i  │          │               │                │
│ v  │          │               │                │
│ t  │          │               │                │
│ y  │          │               │                │
├────┴──────────┴───────────────┴────────────────┤
│ 🤖 AIに指示を入力...              [▲展開] [送信] │  ← 通常時：1行バー（全幅）
└─────────────────────────────────────────────────┘

  ▲展開 すると：

├─────────────────────────────────────────────────┤
│ ● 会話履歴 / ストリーミング出力                   │
│   - ツール実行カード（コマンド・差分・承認ボタン）│   ← 展開時：全幅パネル
│   - エージェントの思考/結果                       │
│ ─────────────────────────────────────────────── │
│ 🤖 AIに指示を入力...              [▼閉じる][送信] │
└─────────────────────────────────────────────────┘
```

### 各領域
| 領域 | 内容 | 幅/高さ |
|------|------|---------|
| ActivityBar | アイコン縦並び（エクスプローラ/検索/AIセッション/設定 等）。サイドバー内容を切替 | 固定 48px |
| Sidebar | 既定は FolderTree。ActivityBarの選択で内容差替（拡張ポイント） | 可変・GridSplitter |
| Terminal | sk0ya.Terminal をホスト。複数タブ対応可 | 可変・GridSplitter |
| Editor | sk0ya.Editor をホスト。複数タブ（ファイル）対応 | 可変・残り全幅 |
| AIバー | 下部全幅。通常1行／展開でパネル化（GridSplitterで高さ調整） | 既定 48px ⇔ 展開時 ~35% |

### レイアウト実装方針
- ルートは `Grid`（行: メイン領域 / AIバー、列内はネストGrid）+ `GridSplitter` でリサイズ可能に。
- 後述の「パネル登録方式」により、各セルの中身を直接ハードコードせず `ContentControl` + ViewModel で差し込む。
- AIバーの展開/縮小は ViewModel の状態 + `RowDefinition` の高さアニメーションで表現。
- 配色・角丸・余白は `ResourceDictionary`（テーマ）に集約し、ダーク/ライト切替に対応。

---

## 5. プロジェクト構成

```
sk0ya.Loomo.sln
├─ src/
│  ├─ Loomo.App/            ← WPF本体（エントリ・DI・Views・ViewModels・Themes）
│  │   ├─ App.xaml(.cs)           ← Host構築・DI登録
│  │   ├─ Views/
│  │   │   ├─ ShellWindow.xaml    ← ルートレイアウト
│  │   │   ├─ ActivityBarView
│  │   │   ├─ FolderTreeView
│  │   │   ├─ TerminalHostView
│  │   │   ├─ EditorHostView
│  │   │   └─ AiBarView
│  │   ├─ ViewModels/
│  │   ├─ Themes/                 ← Dark.xaml / Light.xaml / Controls.xaml
│  │   └─ Behaviors/ Converters/
│  │
│  ├─ Loomo.Core/           ← ドメイン（UI非依存・テスト対象の中核）
│  │   ├─ Agent/                  ← AgentOrchestrator・会話モデル
│  │   ├─ Tools/                  ← IAgentTool・ToolRegistry・各ツール
│  │   └─ Abstractions/           ← ITerminalService 等のインターフェース
│  │
│  ├─ Loomo.Ai/             ← IAiClient の Claude 実装
│  │
│  └─ Loomo.Services/       ← sk0ya.* アダプタ / FileSystem 実装
│
└─ tests/
   └─ Loomo.Tests/         ← Core / Tools のユニットテスト
```

> ポイント: **Loomo.Core は WPF を参照しない。** エージェントの頭脳とツールはUIから独立させ、単体テスト可能にする。

---

## 6. 主要コンポーネント設計

### 6.1 サービス抽象（Abstractions）

```csharp
public interface ITerminalService
{
    Task<CommandResult> RunCommandAsync(string command, CancellationToken ct);
    void Write(string text);                 // 入力流し込み
    event EventHandler<string> OutputReceived;
    Task CancelAsync();
}

public interface IEditorService
{
    Task OpenFileAsync(string path);
    Task<string> GetActiveContentAsync();
    Task ShowDiffAsync(string path, string proposedContent);   // 差分プレビュー
    Task<bool> ApplyEditAsync(string path, string newContent); // 適用（要承認）
    string? ActiveFilePath { get; }
}

public interface IWorkspaceService
{
    string? RootPath { get; }
    string? SelectedPath { get; }            // FolderTreeの選択
    Task<IReadOnlyList<FileNode>> ListAsync(string path);
    event EventHandler<string?> SelectionChanged;
}
```

これらの **実装** は `Loomo.Services` に置き、sk0ya.Terminal / sk0ya.Editor のコントロールをラップする。
（sk0ya.* の実APIに合わせてアダプタ内で吸収。API差異の影響をこの層に閉じ込める。）

### 6.2 AIクライアント

```csharp
public interface IAiClient
{
    IAsyncEnumerable<AgentEvent> StreamAsync(
        Conversation conversation,
        IReadOnlyList<ToolDefinition> tools,
        CancellationToken ct);
}
// AgentEvent = TextDelta | ToolUseRequested | TurnCompleted ...
```

ストリーミングで `TextDelta`（逐次テキスト）と `ToolUseRequested`（ツール呼び出し要求）を流す。

---

## 7. AI連携設計（最重要）

### 7.1 ツール（Tool）= AIの手足

AIエージェントは Claude の **Tool Use（function calling）** を使い、以下のツールを呼び出す。各ツールはサービスを叩くだけの薄いラッパ。

| ツール名 | 接続先サービス | 説明 | 承認 |
|----------|----------------|------|------|
| `list_directory` | IWorkspaceService | フォルダ内容の取得 | 不要 |
| `read_file` | IEditorService/FS | ファイル読み取り | 不要 |
| `open_in_editor` | IEditorService | Editorで開く | 不要 |
| `propose_edit` | IEditorService | 差分を提示（適用は承認後） | **必要** |
| `run_command` | ITerminalService | Terminalでコマンド実行 | **必要** |
| `get_selection` | IWorkspaceService | FolderTreeの現在選択を取得 | 不要 |

```csharp
public interface IAgentTool
{
    string Name { get; }
    ToolDefinition Definition { get; }              // JSON Schema（Claudeへ渡す）
    bool RequiresApproval { get; }
    Task<ToolResult> ExecuteAsync(JsonElement args, CancellationToken ct);
}
```

新しい能力の追加 = `IAgentTool` を実装して DI 登録するだけ（**拡張ポイント**）。

### 7.2 エージェントループ（AgentOrchestrator）

```
ユーザー入力（下部AIバー）
        │
        ▼
[1] Conversation に user メッセージ追加
        │
        ▼
[2] IAiClient.StreamAsync(会話, ツール定義)
        │
        ├── TextDelta ───────────► AIバーに逐次表示
        │
        └── ToolUseRequested
                │
                ▼
        [3] RequiresApproval?
                ├─ Yes → ApprovalService → AIバーに承認カード表示 → ユーザー判断
                └─ No  → そのまま実行
                │
                ▼
        [4] ToolRegistry.Get(name).ExecuteAsync(args)
                │  （= Terminal実行 / Editor操作 / FS読取）
                ▼
        [5] ToolResult を Conversation に追加 → [2] へ戻る
        │
        ▼
最終テキストで完了（TurnCompleted）
```

- ループ・状態は `Loomo.Core` 側に持たせ、UIはイベント購読のみ。
- 中断（キャンセル）対応：`CancellationToken` を全層に伝播。

### 7.3 シーケンス例：「このプロジェクトをビルドしてエラーがあれば直して」

```
User → AIバー：「ビルドしてエラーを直して」
Agent → run_command("dotnet build")        [承認] → Terminalで実行
Terminal → 出力（エラー2件）→ ToolResult
Agent → read_file("Foo.cs")                → 内容取得
Agent → propose_edit("Foo.cs", 修正案)     [承認] → Editorで差分表示
User  → 承認
Agent → run_command("dotnet build")        [承認] → 再ビルド成功
Agent → 「修正完了。ビルドが通りました」    → AIバーに表示
```

人間が同じ作業をする時の経路（FolderTree→Terminal→Editor）と完全に一致しているのがポイント。

---

## 8. 拡張性の設計

| 拡張軸 | 仕組み |
|--------|--------|
| パネル追加 | `IPanelContribution { Id, Title, Icon, Location, CreateViewModel() }` を実装しDI登録。ActivityBar/Sidebar/中央に動的配置 |
| AIツール追加 | `IAgentTool` 実装 + DI登録（ToolRegistryが自動収集） |
| AIプロバイダ差替 | `IAiClient` の別実装に差替（OpenAI / ローカルLLM 等） |
| テーマ | `ResourceDictionary` の差替（Dark/Light/カスタム） |
| コマンド/ショートカット | `ICommandContribution` でコマンドパレット・キーバインド登録 |
| 設定 | `appsettings.json` + ユーザー設定ファイル（APIキーは安全に保管：DPAPI/資格情報マネージャ） |

> 将来的に「ドッキング自由化」が必要なら、固定Grid配置を **AvalonDock (Dirkster.AvalonDock)** に置き換え可能な設計（中身はViewModelなので移行容易）。

---

## 9. 状態管理・DI・ライフサイクル

- `App.xaml.cs` で `Host` を構築し、サービス／ViewModel／ツールをすべてDI登録。
- スコープ：サービスは概ね Singleton（Terminal/Editor/Workspace/AiClient/ToolRegistry）。ViewModelは用途に応じ Singleton/Transient。
- 会話状態は `ConversationStore`（Singleton）に集約。AIセッションの保存/復元に対応（ActivityBarの「AIセッション」から履歴閲覧）。

---

## 10. セキュリティ / 安全設計

- **コマンド実行とファイル書込は既定で承認制**（`ApprovalService`）。AIバーの承認カードで内容を確認してから実行。
- 「自動承認」モード（信頼ワークスペース向け）は設定でオプトイン。危険コマンドのブロックリスト/許可リストを用意。
- 実行スコープはワークスペースルート配下に限定（パストラバーサル防止）。
- APIキーは平文保存しない（Windows資格情報マネージャ / DPAPI）。
- すべてのツール実行はログに記録（監査・再現用）。

---

## 11. 開発ロードマップ

| Phase | 目標 | 主な成果物 |
|-------|------|-----------|
| 0. 土台 | ソリューション構成・DI・空のShell | 4ペイン+AIバーの空レイアウト、テーマ |
| 1. 各ペイン統合 | sk0ya.Terminal / Editor / FolderTree を表示・操作 | 人間が手動で使える状態 |
| 2. サービス層 | ITerminalService/IEditorService/IWorkspaceService 実装 | UIをサービス越しに操作可能に |
| 3. AI最小ループ | IAiClient(Claude) + テキスト往復（ツールなし） | AIバーで会話できる |
| 4. ツール連携 | run_command / read_file / open_in_editor | AIがTerminal/Editorを駆動 |
| 5. 承認 & 編集 | propose_edit + ApprovalService + 差分UI | 安全な自律編集 |
| 6. 仕上げ | セッション保存・設定・テーマ・拡張ポイント整備 | 拡張可能なv1 |

---

## 12. 決定事項 / 未決事項

### 決定済み（2026-05-31）
1. **.NET バージョン**：**.NET 9**（`net9.0` / WPFは `net9.0-windows`）。
2. **sk0ya.Terminal / sk0ya.Editor**：**WPFコントロールとして直接埋め込む**。両パッケージはユーザー本人が開発しており、必要なら **API側を変更可能**。→ アダプタ層は「理想的なサービスIF」を先に定義し、コントロール側を歩み寄らせる方針。
3. **AIプロバイダ**：**ローカルLLM / Claude / OpenAI / GitHub Copilot の4種を切替可能**にする。`IAiClient` 抽象 + プロバイダ実装で対応。実装難度が高いものは後回し可（まず Claude を実装、他はスタブ→順次）。

### AIプロバイダ設計（追補）
```
IAiClient（抽象）
 ├─ ClaudeAiClient        （Anthropic API / Tool Use）   ← 最初に実装
 ├─ OpenAiClient          （OpenAI / function calling）
 ├─ CopilotAiClient       （GitHub Copilot）
 └─ LocalLlmClient        （Ollama 等 OpenAI互換エンドポイント）
```
- プロバイダ選択は設定 + ActivityBar/設定画面から切替。`IAiClientFactory` が現在の設定に応じて実装を返す。
- OpenAI互換API（OpenAI本体・ローカルLLM・一部Copilot経路）は共通基底で実装コスト削減。

### 未決事項（残）
1. **アプリ名 / ルート名前空間** = **Loomo** / `sk0ya.Loomo`（決定済）。
2. **Terminalの位置**：当初案どおり「中央=Terminal / 右=Editor」で確定か、VS Code風に「中央=Editor / Terminal下段」も検討するか。
3. **承認の既定**：初期は全コマンド承認制で良いか。

---

## 付録: 依存パッケージ（想定）

```
CommunityToolkit.Mvvm
Microsoft.Extensions.Hosting
Microsoft.Extensions.DependencyInjection
Microsoft.Extensions.Logging
Microsoft.Extensions.Http
sk0ya.Terminal.Controls   (+ Terminal.Core)
sk0ya.Editor.Controls     (+ Editor.Core, Editor.Controls.Defaults)
（AIは HttpClient 直叩き：Anthropic / OpenAI互換）
```

---

## 13. 実装メモ（2026-05-31 初期実装完了）

実際にソリューションを生成し、ビルド成功（`slnexit=0`）・起動確認済み。実装過程で判明した
**sk0ya パッケージの実APIと設計への影響**を以下に記録する。

### 実パッケージ / 名前空間（要：当初想定と異なる）
| 項目 | 実際 |
|------|------|
| NuGet ID | `sk0ya.Terminal.Controls` / `sk0ya.Editor.Controls`（`.Core`/`.Defaults` を伴う） |
| ルート名前空間 | `Terminal.*` / `Editor.*`（`sk0ya.` 接頭辞は **無い**） |
| ターミナル埋め込みコントロール | **`Terminal.Tabs.TerminalTabView`** (UserControl)、ctor `(commandLine, workingDirectory)` |
| エディタ埋め込みコントロール | **`Editor.Controls.VimEditorControl`** (UserControl)。Vim操作・LSP・Git統合あり |

### 設計への影響（重要）
- **TerminalTabView にコマンド実行APIが追加された（sk0ya.Terminal.Controls 1.0.3）** → 解決済み。
  `Task<TerminalCommandResult> RunCommandAsync(string command, CancellationToken)`、
  `TerminalCommandResult(Command, Output, ExitCode, Completed)`、`bool IsShellIntegrationActive` を公開。
  → エージェントの `run_command` は **可視ターミナルの `RunCommandAsync` へ一本化**（独立 `Process` を廃止）。
    AI が打ったコマンドも人間と同じターミナルに表示・記録される。
  → cwd は shell integration 有効時に `TerminalTabView.WorkingDirectory` から同期、
    無効時は `cd <dir>` 検知でフォールバック追従。`SetWorkingDirectory` はターミナルにも `cd` を反映。
  （旧仕様: 1.0.2 以前は実行APIが無く、`run_command` は独立 `Process`（PowerShell）をバックエンドにしていた。）
- **VimEditorControl** は `LoadFile` / `SetText` / `Text` / `FilePath` / `ExecuteCommand(exコマンド)` を公開。
  IEditorService はこれで実装。ただし **選択テキスト取得APIが無い**ため `get_selection` 相当は将来対応
  （`VimEngine` 経由 or API追加）。

### 現在の到達点（Phase 0〜5 相当が動作）
- 4ペイン+下部AIバー（展開式）レイアウト・ダークテーマ
- FolderTree（フォルダを開く→ツリー表示→ファイル選択でエディタに開く）
- TerminalTabView / VimEditorControl の埋め込み
- エージェントループ（StubAiClient で承認カード含むツール実行まで一通り動作）
- AIプロバイダ4種（Claude実装 / OpenAI・Local共通実装 / Copilotはスタブ）
- 既定プロバイダは **Stub**（APIキー不要で動作確認可能）

### 次の作業候補（すべて完了：2026-05-31）
1. ~~設定画面（プロバイダ切替・APIキー入力／資格情報マネージャ保管）~~ → **完了（§14）**
2. ~~propose_edit の本格的な差分ビュー~~ → **完了（§15）**
3. ~~セッション保存・復元~~ → **完了（§16）**
4. ~~Copilot デバイス認証フロー~~ → **完了（§17）**

### 14. 設定画面（2026-05-31 実装完了）
- **UI**: ActivityBar の ⚙（最下部に配置）で `SettingsView` をサイドバーに表示。`🗂` エクスプローラと
  排他切替（`ShellViewModel.ActivePanel` = `Explorer`/`Settings`、同一アイコン再クリックで閉じる VS Code 風挙動）。
  サイドバーは `SidebarContainer`(Grid) に FolderTree と Settings を重ね、`EnumToVisibilityConverter` で切替。
- **編集項目**: プロバイダ選択（Stub/Claude/OpenAI/Copilot/Local をピル型 RadioButton ＋ `EnumToBoolConverter`）、
  モデル名・APIキー・ベースURL（OpenAI/Local のみ）・最大トークン。短いスカラ項目のみサイドバーで直接編集する。
  プロバイダ毎に項目を出し分け（`ShowApiKey`/`ShowBaseUrl`/`ShowModel`/`ShowCopilotAuth`）。
- **長文項目はエディタ領域で編集**: システムプロンプト・危険コマンド一覧は、狭いサイドバーの複数行 TextBox では
  なく「エディタで編集」ボタンから中央のエディタペイン（`VimEditorControl`）に開く。
  `IEditorService.OpenDocumentAsync(EditorDocument)` が **仮想ドキュメント**（`VimEditorControl.OpenVirtualDocument`、
  `sk0ya.Editor.Controls` 1.0.1 で追加）としてエディタに開く（ディスクには一切書かない）。ユーザーの保存（`:w`）で
  エディタが `SaveRequested(IsVirtual=true, DocumentId)` を発火し、`EditorService` が `DocumentId` でコールバックを引いて
  `EditorDocument.OnSaved` を呼ぶ → `AiSettings` を更新して `settings.json` へ永続化（即時反映）→ `MarkSaved` で
  modified フラグを解除。永続先は `settings.json`（単一の真実）のみ。通常ファイルの `:w` は `EditorService` が
  `VimEditorControl.Save(path)` でディスク保存を委譲する（これにより通常ファイルの `:w` 保存も有効化される）。
  ※ ホスト統合用 API（`Save` / `OpenVirtualDocument` / `MarkSaved` / `IsModified` / `SaveRequestedEventArgs.IsVirtual・
  DocumentId`）は Editor 側へ要望し 1.0.1 で追加済み（`C:\Projects\Editor\docs\HOST_INTEGRATION_REQUESTS.md`）。
- **永続化**: `Loomo.Ai/AiSettingsStore`。保存先 `%APPDATA%/Loomo/settings.json`。**APIキーは DPAPI
  (`ProtectedData`, CurrentUser スコープ) で暗号化**して `apiKeyEnc` に base64 保存（平文保存しない）。
  パッケージ `System.Security.Cryptography.ProtectedData 9.0.0` を Loomo.Ai に追加。`AiSettingsStore` は
  `[SupportedOSPlatform("windows")]`。起動時に `App.OnStartup` で `Load(settings)` して共有 `AiSettings`(Singleton) に反映。
- **反映経路**: 編集→`SettingsViewModel` が共有 `AiSettings` を直接更新→保存。`AiClientFactory.ResolveCurrent()` は
  ターン毎に `AiSettings` を読んでクライアント生成するため**即時反映**。保存時 `Saved` イベントで
  `AiBarViewModel.RefreshProviderLabel()` を呼びAIバーのプロバイダ表示を更新。
- DI: `AiSettingsStore`/`SettingsViewModel` を Singleton 登録。
- 注: その後サイドバーは Explorer/**Sessions**/Settings の3パネルに拡張（§16）。

### 15. propose_edit 差分ビュー（2026-05-31 実装完了）
- **差分計算**: `Loomo.Core/Diff/DiffUtil`（UI非依存）。行単位 LCS で Added/Removed/Context に分類し、
  変更周辺 context=3 行だけ残して長い無変更区間を `Gap`（「… N行省略 …」）に畳む。巨大ファイル（行数積が
  5000² 超）は全削除→全追加にフォールバック。`Stat()` で +/- 行数、`ToUnifiedText()` で +/-/空白/⋯ 接頭辞付き整形。
- **承認カードへの反映**: `ProposeEditTool.DescribeInvocation` が「現在のファイル内容 vs 提案内容」の統合差分を
  サマリ文字列として返す（承認はツール実行前なので `DescribeInvocation` 時点で算出）。`AiBarViewModel.OnApprovalRequested`
  が `tool=="propose_edit"` のとき `TranscriptEntry.SetDiff()` で接頭辞を解析し、`DiffLineVm`（種別→配色）へ展開。
  AIバーの承認カードで緑=追加 / 赤=削除 / 灰=文脈・省略を色分け表示。
- 既存の run_command 等の承認カードは従来どおりプレーン表示（diff 解析しない）。

### 16. セッション保存・復元（2026-05-31 実装完了）
- **永続化**: `Loomo.Core/Agent/ConversationStore`（Singleton, UI非依存）。`%APPDATA%/Loomo/sessions/{id}.json`。
  `Conversation`→専用DTOへマップして System.Text.Json で保存（get-only コレクションの逆シリアライズ回避）。
  タイトルは最初のユーザー発言を40字に丸めて自動生成。`Save`/`Delete` で `Changed` イベント発火。
  ファイル名はサニタイズ（パストラバーサル防止）。
- **UI**: ActivityBar `💬` を **AIセッション一覧パネル**（`SessionsView`/`SessionsViewModel`）に割当て（旧: AIバー展開トグル）。
  新規セッション / クリックで復元 / 🗑 削除。`SidebarPanel` に `Sessions` を追加し3パネル排他切替。
- **AIバー連携**: `AiBarViewModel` の `_conversation` を可変化＋`_currentSessionId` 保持。ターン完了の finally で
  `ConversationStore.Save` 自動アップサート。`StartNewSession()`＝会話/トランスクリプト初期化、
  `RestoreSession(LoadedSession)`＝会話差替え＋`RebuildTranscript()` で表示再構築（動的な承認カードは除く）。
  `store.Changed`→`SessionsViewModel.Refresh`（Dispatcher 経由）。テスト3件（往復/上書き/削除）パス。

### 17. Copilot デバイス認証フロー（2026-05-31 実装完了）
- **OpenAI互換の共通化**: `OpenAiProtocol`（internal static）に Chat Completions のリクエスト組立 (`BuildRequest`) と
  送信→イベント化 (`SendAsync`、認証ヘッダは `Action<HttpRequestMessage> configure` で注入) を抽出。
  `OpenAiCompatibleClient` と `CopilotAiClient` が共用。
- **認証**: `Loomo.Ai/CopilotAuthService`。GitHub OAuth Device Flow（client_id=`Iv1.b507a08c87ecfe98`、scope=`read:user`）。
  `RequestDeviceCodeAsync`→user_code/verification_uri、`PollForAccessTokenAsync`→`authorization_pending`/`slow_down`/
  `expired_token`/`access_denied` を解釈して GitHub トークン(gho_…)を取得。
- **チャット**: `CopilotAiClient` が保存済み gho トークンを `api.github.com/copilot_internal/v2/token` で
  Copilot セッショントークンへ交換（期限までキャッシュ）、`api.githubcopilot.com/chat/completions` を
  `Editor-Version`/`Copilot-Integration-Id`/`Authorization: Bearer` ヘッダ付きで叩く。`AiClientFactory` で実体化（旧スタブ廃止）。
- **UI**: 設定画面で Copilot 選択時は API キー手入力欄を隠し、「GitHub でサインイン」ボタンを表示。
  押下で device code 発行→user_code をクリップボードへコピー＆ブラウザで認証URLを開く→ポーリング→成功で
  gho トークンを `AiSettings.Copilot.ApiKey` に保存（§14 の DPAPI 暗号化で永続化）。キャンセル/サインアウトも可。
- **未検証点**: copilot_internal トークン交換と githubcopilot エンドポイントは非公式仕様のため、実トークンでの
  E2E 動作は未確認（実装はビルド/起動まで検証済み）。Copilot 契約が無いアカウントではトークン交換が 4xx になる想定。

### 18. セキュリティ / 安全設計（2026-05-31 実装完了 = §10 の具体化）
- **安全ポリシー（UI非依存）**: `Loomo.Core/Safety`。`SafetySettings`（`AutoApprove` / `RestrictToWorkspaceRoot` /
  `BlockedCommandPatterns`）と `ISafetyPolicy`→`SafetyPolicy`。`SafetyPolicy.Evaluate(toolName, args)` は `run_command` の
  `command` 引数を危険コマンドのブロックリスト（正規表現・大文字小文字無視）と照合し `SafetyDecision`(Blocked/Reason) を返す。
  設定インスタンスを保持し評価毎に最新値を読むため**設定変更が即時反映**。
- **既定ブロックパターン**: `rm -rf`（-rf/-fr/分割フラグを lookahead で順不同に検知）・`rmdir /s`・`del /s|/q`・
  `Remove-Item -Recurse -Force`・`format C:` 等のディスクフォーマット（`dotnet format` は除外）・`mkfs`・`dd if=`・
  `> /dev/sd`・`shutdown`・`reboot`・フォークボム。
- **エージェントループ連携**: `AgentOrchestrator` に `ISafetyPolicy` を注入。ツール実行前に `Evaluate`→ブロック時は実行せず
  エラー結果（理由付き）をAIへ差し戻し（`ToolExecutionStarted`/`Completed` も発火しトランスクリプトに表示）。承認は
  `tool.RequiresApproval && !AutoApprove` のときのみ（**自動承認モードで承認カードを省略**）。
- **ワークスペーススコープ制限（パストラバーサル防止）**: `IWorkspaceService.ResolvePath(path)` を追加。`WorkspaceService`
  が `SafetySettings` を見て、`RestrictToWorkspaceRoot` 有効かつルート設定済みのとき、解決後の絶対パスがルート配下
  （`IsWithin`：同一 or `root\` 前方一致, OrdinalIgnoreCase）でなければ `UnauthorizedAccessException`。
  `read_file`/`list_directory` は元々 workspace 経由で保護。`open_in_editor`/`propose_edit` は EditorService 直書きを
  `ResolvePath` 経由に変更し、相対パス解決と書込スコープ制限を両立。
- **永続化 / UI**: `SafetySettings` は `AiSettings.Safety` に保持し `AiSettingsStore` が平文で保存（秘匿情報ではない）。
  `ApplyTo` は既存インスタンスを書き換え（DIシングルトン参照を保つ）。DI は `AiSettings.Safety` を Singleton 共有 +
  `ISafetyPolicy`/`SafetyPolicy` 登録。設定画面に「自動承認」「ルート配下に限定」チェックボックスを追加。
  危険コマンドのブロックリストは「エディタで編集」ボタンからエディタペインで編集する（§14 の
  `OpenDocumentAsync`／保存コールバック方式。1行1パターン・正規表現、`#` 始まりはコメント）。
  テスト `SafetyTests` 7件（ブロック/許可/非対象ツール/スコープ）パス、計19件。

---

## 19. 今後の実装構想（2026-06-01 ブレインストーム）

> ここから先は **未確定の構想** メモ。実装済み（§14〜18）とは区別する。優先度・実現性は今後精査。
> 大きな方向性は「**AIエージェントの観測・制御**」「**マルチエージェント**」「**ワークスペース／レイアウトの自由化**」
> 「**集合知（ナレッジ）化**」の4本柱。

### 19.1 エージェントの観測性（Observability）
- **全ログの永続保存**: AIエージェントのやり取り（プロンプト・ツール呼び出し・結果・思考）をすべて構造化して保存する。
  現状のセッション保存（§16）は会話の往復のみ。これを拡張し、ツール実行の引数・出力・所要時間・トークン等まで記録する
  詳細ログ層を別途設ける（セッションJSONとは分離し、解析しやすい形式に）。
- **ログ解析**: 保存したログを後から解析できるようにする（どのツールがどれだけ呼ばれたか、失敗パターン、所要時間など）。
- **動作中エージェントの可視化**: 実行中に「いまどのエージェントが動いているか」を UI で分かるようにする
  （後述のマルチエージェントと連動）。
- **プレイバック**: 保存したログ／セッションを再生（タイムライン的に追体験）できるようにする。
  ※ §3 の「AIも人間も同じ経路で操作する」設計が、記録・再現を容易にする前提として効いてくる。

### 19.2 マルチエージェント
- **子エージェントの設定**: 親エージェントが子（サブ）エージェントを構成・起動できるようにする。
- **稼働状況の可視化**: 複数エージェントが動くとき、どれがアクティブかを UI で識別できるようにする（19.1 と共通）。

### 19.3 ワークスペース／プロジェクト管理
- **ワークスペースのタブ切替**: 複数ワークスペースをタブで切り替えられるようにする。
- **サイドバーでのプロジェクト管理**: 個別ワークスペースだけでなく、**全体のプロジェクト群を横断管理**する
  ビューをサイドバーに持たせる構想。

### 19.4 FolderTree 強化
- **ignore の非表示**: `.gitignore` 等で無視されるファイルを隠すオプション。→ **完了（2026-06-01）**
  FolderTree 上部のコンパクトなツールバーにトグルとして配置し、`git check-ignore --stdin` によるバッチ判定で表示対象を絞り込む。
- **差分のみ表示フィルター**: 変更があったファイルだけを表示するフィルター。→ **完了（2026-06-01）**
  `git status --porcelain -z --untracked-files=all` を用いて変更ファイルとその親ディレクトリだけを表示する。
- **自動更新**: ワークスペース配下を `FileSystemWatcher` で監視し、作成・削除・変更・リネーム時に debounce 付きで Git 状態とツリーを再読込する。→ **完了（2026-06-01）**
- **汎用フィルター機能**: 名前・種別などでツリーを絞り込む。

### 19.5 レイアウト／タブの自由化（ツールとしての強み）
- **レイアウトをユーザーが決める**: 固定 Grid（§4）から、ユーザーがペイン配置を自由に決められる方式へ
  （§8 で言及済みの AvalonDock 化が候補）。
- **Editor・ターミナル・ブラウザを自由配置**: これらを自由に開閉・配置できるのがこのツールの強み。
  - **Web タブの追加**: ブラウザペイン（Web タブ）を新設し、Editor／Terminal と並べる。
  - **タブの AI 管理**: 各ペインがタブを持つが、その開閉・整理を **AI が管理**できると良い（システマチックに実現可能そう）。
  - **AI への「開く」指示**: 「これを開いて」と AI に指示して、対象（ファイル/URL/マップ等）を適切なペインで開かせる。
- **全タブのサイドバー集約**: ターミナル・ブラウザ・Editor のタブをすべてサイドバーに集める案。
  - **アプリのカスタム埋め込み**: 任意アプリを埋め込めるようにする。Windows 側で DLL を作れば実現できそう。
- **画面分割**: ターミナル／Editor／ブラウザはそれぞれ分割表示可能に。
- **Zen モード**: 単一ペインを全画面化して集中できるモード。

### 19.6 集合知（ナレッジ）化
- **AI へのインプットがそのまま集合知に**: ユーザーが AI に与えた情報を蓄積・再利用できる知識ベースにする。
  - Web のリンク収集、**ブックマークの更新**。
  - **マインドマップの特定マップを開く**といった連携も視野。

### 19.7 ランチャー
- **ランチャーからの指示**: アプリ外（ランチャー）から AI へ指示を投げられるようにする。

### 19.8 設定 UI の見直し
- 現状サイドバーで編集（§14）。項目が増えるなら **専用の設定 UI（別画面）** に切り出す方が良いか要検討。

---

## 20. 観測性＋プレイバック 詳細設計（2026-06-01 構想深掘り）

> §19.1 / §19.1（プレイバック）の深掘り。**未確定の設計案**だが、既存コード（`AgentOrchestrator` /
> `AgentEvent` / `ConversationStore`）への接続点まで具体化する。マルチエージェント可視化（§19.2）の土台でもある。

### 20.1 中心となる気づき：「最終状態」ではなく「イベントストリーム」を保存する
- 現状 `ConversationStore`（§16）が保存するのは `Conversation.Messages`＝**最終状態のスナップショット**のみ。
  これは「再開（resume）」には十分だが、観測・解析・再生には **以下を取りこぼしている**：
  - 各処理のタイミング（time-to-first-token / ツール所要時間 / ターン全体の所要）
  - 使用プロバイダ・モデル（ターン毎に `AiClientFactory.ResolveCurrent()` で変わり得る）
  - 安全ポリシーのブロック有無と理由（`SafetyPolicy.Evaluate` の結果）
  - 承認の可否と待ち時間（`ApprovalRequested` → 人間の判断）
  - ループ反復回数（`MaxIterations=25` のどこで終わったか）
  - エラー（`AgentError`）、中断（`OperationCanceledException`）
  - トークン/コスト（現状クライアントが表に出していない＝既知の制約）
- 一方 `AgentOrchestrator.RunTurnAsync` は **すでに豊富な `AgentEvent` ストリームを流している**
  （`TextDelta` / `ToolUseRequested` / `ToolExecutionStarted` / `ToolExecutionCompleted` /
  `ApprovalRequested` / `TurnCompleted` / `AgentError`）。
  → **観測性の核心 = この順序付きイベント列を追記専用ログとして永続化すること。**
  セッションJSON（再開用の状態）とは **別ライフサイクル・別ファイル**として分離する。

### 20.2 ストレージ：セッション毎の追記専用 JSON Lines
- 形式: **JSONL**（1行1イベント）。`%APPDATA%/Loomo/traces/{sessionId}.jsonl`。
  - 追記専用 → クラッシュ耐性・ストリーム書き込み・順序保証・末尾 tail で「実況」可能。
  - `sessions/{id}.json`（§16）は従来どおり **再開用スナップショット**として残す（役割分担）。
- 大きなペイロード（ツール出力・ファイル内容）は、小さければインライン、大きければ
  `traces/{sessionId}/blobs/{sha256}` にスピルしてハッシュ参照（JSONL を走査可能なサイズに保つ）。

### 20.3 トレースイベントのスキーマ（封筒＋種別）
```
封筒(envelope): { ts, seq, sessionId, turnId, agentId, kind, payload }
   agentId は単一エージェント期は "root" 固定。§19.2 で複数エージェントが同一ログに interleave。
```
種別（kind）と payload 例：
| kind | payload | 取得元 |
|------|---------|--------|
| `session.started` | workspaceRoot, provider, model, settingsSnapshot | ターン開始時（新規セッション） |
| `turn.started` | turnId, userInput, provider, model | `RunTurnAsync` 冒頭 |
| `ai.message` | fullText（TextDelta を合算） | ストリーム終了時 |
| `ai.tool_use` | toolUseId, name, argsJson | `ToolUseRequested` |
| `safety.evaluated` | tool, blocked, reason | `SafetyPolicy.Evaluate` 後 |
| `approval.requested` | tool, summary | `ApprovalRequested` |
| `approval.resolved` | tool, approved, waitMs | 承認完了時 |
| `tool.started` | toolUseId, name | `ToolExecutionStarted` |
| `tool.completed` | toolUseId, name, isError, durationMs, contentLen, contentRef | `ToolExecutionCompleted` |
| `ai.usage` | inputTokens, outputTokens, costEstimate | クライアント（要拡張・§20.7） |
| `turn.completed` | finalText, iterations, durationMs | `TurnCompleted` |
| `error` | message, where | `AgentError` / 例外 |

### 20.4 フック方法（既存への侵襲を最小化）
- **`ITraceSink` を `AgentOrchestrator` にDI注入**する案を推奨。
  - `ITraceSink.Record(TraceEvent)`（fire-and-forget、内部でバッファ＋非同期フラッシュ）。
  - Core を汚さないよう **`NullTraceSink.Instance`（no-op）を既定**にし、既存テストはそのまま通す。
  - UI 向けの `AgentEvent` ストリームは**一切変えない**（`AiBarViewModel` は無改修）。
- 単純な「`RunTurnAsync` の yield をデコレータで tee する」案だけでは、`AgentEvent` に乗っていない
  情報（プロバイダ/モデル・安全ブロック理由・承認待ち時間・各種 duration・トークン）が取れない。
  → orchestrator 内部の既知ポイントで `_trace.Record(...)` を呼ぶ方式が確実。
- 実 IO 実装 `JsonlTraceSink` は **Loomo.Core に置いて可**（`ConversationStore` が既に Core で File IO を
  しており一貫する）。DI で `ITraceSink` をシングルトン登録（`App.xaml.cs`）。

### 20.5 解析層
- `TraceReader`：JSONL を読み、型付き `TraceEvent` を列挙。
- **セッション単位メトリクス**（`SessionMetrics`）：ツール呼び出し回数・成功/失敗率・総所要・
  平均ツール所要・ターン毎反復数・time-to-first-token・（取れれば）総トークン/コスト。
  → Sessions パネルに「セッション分析」タブとして表示。
- **横断分析**（後続）：失敗の多いツール、期間別トークン消費など。JSONL 全走査が重くなったら
  SQLite インデックスを導入（まずはオンデマンド走査で開始）。

### 20.6 プレイバック
- §3 の不変条件（AIも人間も同じ経路で操作）＋ トレースが順序付きイベント列であることにより、
  プレイバック = **トレースをタイムライン上に再生して読み取り専用トランスクリプトを再構成**する。
  - **モードA：トランスクリプト再生（安全・v1）**。`TranscriptEntry` 描画を再利用し、
    元のタイミング（または step 実行）で会話を再構築。**ツールは再実行しない**
    （`run_command` の再実行は破壊的になり得るため）。
  - **モードB：再実行/フォーク（高度・危険・将来）**。ユーザー入力を実エージェントに再投入して
    再現する。明示オプトイン＋サンドボックス前提。v1 はスコープ外として記載のみ。
- UI：Sessions パネル内に Playback ビュー（スクラバー/タイムライン・再生/一時停止/ステップ・速度）。

### 20.7 既知制約・横断事項
- **トークン/コスト**：現状クライアントは usage を表に出していない（§AI clients の既知制約）。
  `ai.usage` を埋めるにはクライアント側の応答パースに usage 抽出を足す必要がある（別フェーズ）。
- **プライバシー/サイズ**：トレースはプロンプト・ファイル内容・コマンド出力を含む機微データ。
  既定オプトアウト可、保持ポリシー（件数/日数上限でローテーション）、サイズ上限を設ける。
  API キーは元々 payload に載らない。
- **マルチエージェント前方互換**：封筒に `agentId` を最初から持たせれば、§19.2 到達時に同一ログへ
  自然に interleave でき、§19.1「動作中エージェントの可視化」は **トレース末尾のライブ tail を
  agentId/状態でフィルタするだけ**で実現できる。観測性が §19.2 の土台になる所以。

### 20.8 段階導入（フェーズ）
| Phase | 内容 | 主な変更点 |
|-------|------|-----------|
| A（土台）| `ITraceSink` + `JsonlTraceSink` を orchestrator に配線。既存イベント＋タイミング＋プロバイダ/モデル＋安全/承認結果を記録。UIなし | `AgentOrchestrator` に `ITraceSink` 注入、`NullTraceSink` 既定、DI登録 |
| B（解析）| `TraceReader` + セッション分析タブ（回数/所要/失敗、取れればトークン） | `SessionMetrics`、Sessions パネル拡張 |
| C（再生）| プレイバック（モードA：トランスクリプト再生） | `PlaybackView` / `PlaybackViewModel`、`TranscriptEntry` 再利用 |
| D（拡張）| トークン/コスト表示（クライアント拡張）＋横断分析 | 各 `IAiClient` の usage 抽出、必要なら SQLite |

### 20.9 想定ファイル
- 新規：`Loomo.Core/Observability/{TraceEvent.cs, ITraceSink.cs, NullTraceSink.cs, JsonlTraceSink.cs, TraceReader.cs, SessionMetrics.cs}`
- 改修：`Loomo.Core/Agent/AgentOrchestrator.cs`（`ITraceSink` 注入＋記録点）、`App.xaml.cs`（DI登録）
- UI：`SessionsViewModel`/`SessionsView` 拡張、`PlaybackView`/`PlaybackViewModel` 追加
