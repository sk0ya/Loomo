# 04. 機能詳細（設定・差分・セッション・観測性・コンポーザ/ペグボード）

> 章番号（§N）は設計書全体で通し。他章の §N がどのファイルかは [README](README.md) の「§→ファイル対応表」を参照。

### 14. 設定画面（2026-05-31 実装完了）
- **UI**: ActivityBar の ⚙（最下部に配置）で `SettingsView` をサイドバーに表示。`🗂` エクスプローラと
  排他切替（`ShellViewModel.ActivePanel` = `Explorer`/`Settings`、同一アイコン再クリックで閉じる VS Code 風挙動）。
  サイドバーは `SidebarContainer`(Grid) に FolderTree と Settings を重ね、`EnumToVisibilityConverter` で切替。
- **編集項目**: モデル（ローカルモデルフォルダ／`.gguf` の選択）・モデル保存先フォルダ・APIキー（ローカル推論では不要）・
  最大トークン。短いスカラ項目のみサイドバーで直接編集する。モデル一覧は `ModelCatalogService` が
  `%APPDATA%/Loomo/models/` 配下の ONNX フォルダ（`genai_config.json` 持ち）／GGUF を列挙する（HTTP 無し）。
  ダウンロード用ドロップダウン＋ダウンロードボタンで `ModelDownloadService.Catalog` のモデルを取得できる。
  ※ ローカルLLM専用化（§22→§25）に伴い、旧プロバイダ選択 UI（Stub/Claude/OpenAI/Copilot/Local のピル型
  RadioButton）と Ollama ホスト URL・`Thinking` チェックボックスは撤去された（`baseUrl`/`thinking` は
  `settings.json` から後方互換で読むのみ）。
- **長文項目はエディタ領域で編集**: システムプロンプト・危険コマンド一覧は、狭いサイドバーの複数行 TextBox では
  なく「エディタで編集」ボタンから中央のエディタペイン（`VimEditorControl`）に開く。
  `IEditorService.OpenDocumentAsync(EditorDocument)` が **仮想ドキュメント**（`VimEditorControl.OpenVirtualDocument`、
  `sk0ya.Editor.Controls` 1.0.1 で追加）としてエディタに開く（ディスクには一切書かない）。ユーザーの保存（`:w`）で
  エディタが `SaveRequested(IsVirtual=true, DocumentId)` を発火し、`EditorService` が `DocumentId` でコールバックを引いて
  `EditorDocument.OnSaved` を呼ぶ → `AiSettings` を更新して `settings.json` へ永続化（即時反映）→ `MarkSaved` で
  modified フラグを解除。永続先は `settings.json`（単一の真実）のみ。通常ファイルの `:w` は `EditorService` が
  `VimEditorControl.Save(path)` でディスク保存を委譲する（これにより通常ファイルの `:w` 保存も有効化される）。
  ※ ホスト統合用 API（`Save` / `OpenVirtualDocument` / `MarkSaved` / `IsModified` / `SaveRequestedEventArgs.IsVirtual・
  DocumentId`）は Editor 側へ要望し 1.0.1 で追加済み（`C:\Projects\Editor\docs\HOST_INTEGRATION_REQUESTS.md`）。
- **永続化**: `Loomo.Ai/AiSettingsStore`。保存先 `%APPDATA%/Loomo/settings.json`。**APIキーは DPAPI
  (`ProtectedData`, CurrentUser スコープ) で暗号化**して `apiKeyEnc` に base64 保存（平文保存しない）。
  パッケージ `System.Security.Cryptography.ProtectedData 9.0.0` を Loomo.Ai に追加。`AiSettingsStore` は
  `[SupportedOSPlatform("windows")]`。起動時に `App.OnStartup` で `Load(settings)` して共有 `AiSettings`(Singleton) に反映。
- **反映経路**: 編集→`SettingsViewModel` が共有 `AiSettings` を直接更新→保存。`AiClientFactory.ResolveCurrent()` は
  ターン毎に `AiSettings` を読んでクライアント生成するため**即時反映**。保存時 `Saved` イベントで
  `AiBarViewModel.RefreshProviderLabel()` を呼びAIバーのプロバイダ表示を更新。
- DI: `AiSettingsStore`/`SettingsViewModel` を Singleton 登録。
- 注: その後サイドバーは Explorer/**Sessions**/Settings の3パネルに拡張（§16）。

### 15. propose_edit 差分ビュー（2026-05-31 実装完了）
- **差分計算**: `Loomo.Core/Diff/DiffUtil`（UI非依存）。行単位 LCS で Added/Removed/Context に分類し、
  変更周辺 context=3 行だけ残して長い無変更区間を `Gap`（「… N行省略 …」）に畳む。巨大ファイル（行数積が
  5000² 超）は全削除→全追加にフォールバック。`Stat()` で +/- 行数、`ToUnifiedText()` で +/-/空白/⋯ 接頭辞付き整形。
- **承認カードへの反映**: `ProposeEditTool.DescribeInvocation` が「現在のファイル内容 vs 提案内容」の統合差分を
  サマリ文字列として返す（承認はツール実行前なので `DescribeInvocation` 時点で算出）。`AiBarViewModel.OnApprovalRequested`
  が `tool=="propose_edit"` のとき `TranscriptEntry.SetDiff()` で接頭辞を解析し、`DiffLineVm`（種別→配色）へ展開。
  AIバーの承認カードで緑=追加 / 赤=削除 / 灰=文脈・省略を色分け表示。
- 既存の run_command 等の承認カードは従来どおりプレーン表示（diff 解析しない）。

### 16. セッション保存・復元（2026-05-31 実装完了）
- **永続化**: `Loomo.Core/Agent/ConversationStore`（Singleton, UI非依存）。`%APPDATA%/Loomo/sessions/{id}.json`。
  `Conversation`→専用DTOへマップして System.Text.Json で保存（get-only コレクションの逆シリアライズ回避）。
  タイトルは最初のユーザー発言を40字に丸めて自動生成。`Save`/`Delete` で `Changed` イベント発火。
  ファイル名はサニタイズ（パストラバーサル防止）。
- **UI**: ActivityBar `💬` を **AIセッション一覧パネル**（`SessionsView`/`SessionsViewModel`）に割当て（旧: AIバー展開トグル）。
  新規セッション / クリックで復元 / 🗑 削除。`SidebarPanel` に `Sessions` を追加し3パネル排他切替。
- **AIバー連携**: `AiBarViewModel` の `_conversation` を可変化＋`_currentSessionId` 保持。ターン完了の finally で
  `ConversationStore.Save` 自動アップサート。`StartNewSession()`＝会話/トランスクリプト初期化、
  `RestoreSession(LoadedSession)`＝会話差替え＋`RebuildTranscript()` で表示再構築（動的な承認カードは除く）。
  `store.Changed`→`SessionsViewModel.Refresh`（Dispatcher 経由）。テスト3件（往復/上書き/削除）パス。

## 20. 観測性＋プレイバック 詳細設計（2026-06-01 構想深掘り）

> §19.1 / §19.1（プレイバック）の深掘り。**未確定の設計案**だが、既存コード（`AgentOrchestrator` /
> `AgentEvent` / `ConversationStore`）への接続点まで具体化する。マルチエージェント可視化（§19.2）の土台でもある。

### 20.1 中心となる気づき：「最終状態」ではなく「イベントストリーム」を保存する
- 現状 `ConversationStore`（§16）が保存するのは `Conversation.Messages`＝**最終状態のスナップショット**のみ。
  これは「再開（resume）」には十分だが、観測・解析・再生には **以下を取りこぼしている**：
  - 各処理のタイミング（time-to-first-token / ツール所要時間 / ターン全体の所要）
  - 使用プロバイダ・モデル（ターン毎に `AiClientFactory.ResolveCurrent()` で変わり得る）
  - 安全ポリシーのブロック有無と理由（`SafetyPolicy.Evaluate` の結果）
  - 承認の可否と待ち時間（`ApprovalRequested` → 人間の判断）
  - ループ反復回数（`MaxIterations=25` のどこで終わったか）
  - エラー（`AgentError`）、中断（`OperationCanceledException`）
  - トークン/コスト（※後述：現在は `AiUsageReported` でトークン数と load/prefill/decode のタイミングを取得済み）
- 一方 `AgentOrchestrator.RunTurnAsync` は **すでに豊富な `AgentEvent` ストリームを流している**
  （`TextDelta` / `ToolUseRequested` / `ToolExecutionStarted` / `ToolExecutionCompleted` /
  `ApprovalRequested` / `TurnCompleted` / `AgentError`）。
  → **観測性の核心 = この順序付きイベント列を追記専用ログとして永続化すること。**
  セッションJSON（再開用の状態）とは **別ライフサイクル・別ファイル**として分離する。

### 20.2 ストレージ：セッション毎の追記専用 JSON Lines
- 形式: **JSONL**（1行1イベント）。`%APPDATA%/Loomo/traces/{sessionId}.jsonl`。
  - 追記専用 → クラッシュ耐性・ストリーム書き込み・順序保証・末尾 tail で「実況」可能。
  - `sessions/{id}.json`（§16）は従来どおり **再開用スナップショット**として残す（役割分担）。
- 大きなペイロード（ツール出力・ファイル内容）は、小さければインライン、大きければ
  `traces/{sessionId}/blobs/{sha256}` にスピルしてハッシュ参照（JSONL を走査可能なサイズに保つ）。

### 20.3 トレースイベントのスキーマ（封筒＋種別）
```
封筒(envelope): { ts, seq, sessionId, turnId, agentId, kind, payload }
   agentId は単一エージェント期は "root" 固定。§19.2 で複数エージェントが同一ログに interleave。
```
種別（kind）と payload 例：
| kind | payload | 取得元 |
|------|---------|--------|
| `session.started` | workspaceRoot, provider, model, settingsSnapshot | ターン開始時（新規セッション） |
| `turn.started` | turnId, userInput, provider, model | `RunTurnAsync` 冒頭 |
| `ai.message` | fullText（TextDelta を合算） | ストリーム終了時 |
| `ai.tool_use` | toolUseId, name, argsJson | `ToolUseRequested` |
| `safety.evaluated` | tool, blocked, reason | `SafetyPolicy.Evaluate` 後 |
| `approval.requested` | tool, summary | `ApprovalRequested` |
| `approval.resolved` | tool, approved, waitMs | 承認完了時 |
| `tool.started` | toolUseId, name | `ToolExecutionStarted` |
| `tool.completed` | toolUseId, name, isError, durationMs, contentLen, contentRef | `ToolExecutionCompleted` |
| `ai.usage` | inputTokens, outputTokens, loadMs/promptEvalMs/evalMs | `AiUsageReported`（`OnnxGenAiEngine` が自己計測・実装済み・§20.7） |
| `turn.completed` | finalText, iterations, durationMs | `TurnCompleted` |
| `error` | message, where | `AgentError` / 例外 |

### 20.4 フック方法（既存への侵襲を最小化）
- **`ITraceSink` を `AgentOrchestrator` にDI注入**する案を推奨。
  - `ITraceSink.Record(TraceEvent)`（fire-and-forget、内部でバッファ＋非同期フラッシュ）。
  - Core を汚さないよう **`NullTraceSink.Instance`（no-op）を既定**にし、既存テストはそのまま通す。
  - UI 向けの `AgentEvent` ストリームは**一切変えない**（`AiBarViewModel` は無改修）。
- 単純な「`RunTurnAsync` の yield をデコレータで tee する」案だけでは、`AgentEvent` に乗っていない
  情報（プロバイダ/モデル・安全ブロック理由・承認待ち時間・各種 duration・トークン）が取れない。
  → orchestrator 内部の既知ポイントで `_trace.Record(...)` を呼ぶ方式が確実。
- 実 IO 実装 `JsonlTraceSink` は **Loomo.Core に置いて可**（`ConversationStore` が既に Core で File IO を
  しており一貫する）。DI で `ITraceSink` をシングルトン登録（`App.xaml.cs`）。

### 20.5 解析層
- `TraceReader`：JSONL を読み、型付き `TraceEvent` を列挙。
- **セッション単位メトリクス**（`SessionMetrics`）：ツール呼び出し回数・成功/失敗率・総所要・
  平均ツール所要・ターン毎反復数・time-to-first-token・（取れれば）総トークン/コスト。
  → Sessions パネルに「セッション分析」タブとして表示。
- **横断分析**（後続）：失敗の多いツール、期間別トークン消費など。JSONL 全走査が重くなったら
  SQLite インデックスを導入（まずはオンデマンド走査で開始）。

### 20.6 プレイバック
- §3 の不変条件（AIも人間も同じ経路で操作）＋ トレースが順序付きイベント列であることにより、
  プレイバック = **トレースをタイムライン上に再生して読み取り専用トランスクリプトを再構成**する。
  - **モードA：トランスクリプト再生（安全・v1）**。`TranscriptEntry` 描画を再利用し、
    元のタイミング（または step 実行）で会話を再構築。**ツールは再実行しない**
    （`run_powershell` の再実行は破壊的になり得るため）。
  - **モードB：再実行/フォーク（高度・危険・将来）**。ユーザー入力を実エージェントに再投入して
    再現する。明示オプトイン＋サンドボックス前提。v1 はスコープ外として記載のみ。
- UI：Sessions パネル内に Playback ビュー（スクラバー/タイムライン・再生/一時停止/ステップ・速度）。

### 20.7 既知制約・横断事項
- **トークン/コスト**：ONNX 移行後、`OnnxGenAiEngine` が `Stopwatch` で load/prefill/decode を自己計測し、
  トークン数とともに `AiUsageReported` として流すようになった（AI バーの「📊 AI内訳」にライブ表示・§25）。
  ローカル推論のため金銭コストは無く `costEstimate` は持たない。`ai.usage` トレース行はこの値で埋まる。
- **プライバシー/サイズ**：トレースはプロンプト・ファイル内容・コマンド出力を含む機微データ。
  既定オプトアウト可、保持ポリシー（件数/日数上限でローテーション）、サイズ上限を設ける。
  API キーは元々 payload に載らない。
- **マルチエージェント前方互換**：封筒に `agentId` を最初から持たせれば、§19.2 到達時に同一ログへ
  自然に interleave でき、§19.1「動作中エージェントの可視化」は **トレース末尾のライブ tail を
  agentId/状態でフィルタするだけ**で実現できる。観測性が §19.2 の土台になる所以。

### 20.8 段階導入（フェーズ）
| Phase | 内容 | 主な変更点 |
|-------|------|-----------|
| A（土台）| `ITraceSink` + `JsonlTraceSink` を orchestrator に配線。既存イベント＋タイミング＋プロバイダ＋安全/承認結果を記録。UIなし → **完了（2026-06-01）** | `AgentOrchestrator` に `ITraceSink` 注入、`NullTraceSink` 既定、DI登録。`%APPDATA%/Loomo/traces/{sessionId}.jsonl` へ追記。`AiSettings.Observability`（`EnableTracing`/`MaxSessions`）でオプトアウト・件数ローテーション。モデル名は Core が Ai 層に依存しないため未記録（Phase D で usage と併せて拡張） |
| B（解析）| `TraceReader` + セッション分析タブ（回数/所要/失敗、取れればトークン） | `SessionMetrics`、Sessions パネル拡張 |
| C（再生）| プレイバック（モードA：トランスクリプト再生） | `PlaybackView` / `PlaybackViewModel`、`TranscriptEntry` 再利用 |
| D（拡張）| トークン/コスト表示（クライアント拡張）＋横断分析 | usage 抽出は ONNX 移行で実装済み（`AiUsageReported`・§25）。横断分析／SQLite は未着手 |

### 20.9 想定ファイル
- 新規：`Loomo.Core/Observability/{TraceEvent.cs, ITraceSink.cs, NullTraceSink.cs, JsonlTraceSink.cs, TraceReader.cs, SessionMetrics.cs}`
  - Phase A 実装済：`TraceEvent.cs`（封筒＋`TraceKinds`）、`ITraceSink.cs`、`NullTraceSink.cs`、`JsonlTraceSink.cs`、`ObservabilitySettings.cs`。`TraceReader.cs`/`SessionMetrics.cs` は Phase B。
- 改修：`Loomo.Core/Agent/AgentOrchestrator.cs`（`ITraceSink` 注入＋記録点）、`App.xaml.cs`（DI登録）。
  - Phase A 実装済。あわせて `RunTurnAsync` に `sessionId` 引数を追加（トレースと保存で同一IDを共有）、`AiSettings.Observability` を永続化（`AiSettingsStore`）。
- UI：`SessionsViewModel`/`SessionsView` 拡張、`PlaybackView`/`PlaybackViewModel` 追加（Phase B/C・未着手）。

---

## 23. コマンドジャーナル／コマンドコンポーザ／ペグボード 設計（2026-06-13 構想）

> **非AI機能** 3点の設計案（§19 のブレインストームに無い新規軸＋§19.6 の最小版）。共通の狙いは
> 「Loomo 固有の資産を活かす」こと：①自作 Terminal の **OSC133 シェル統合（コマンド境界）**、
> ②自由レイアウトツリー（§21）、③Vim エディタ。3つは独立に出荷できるが、ジャーナルを軸に相互連携する。

### 23.1 コマンドジャーナル（人間用の観測性）→ **実装後に撤回（2026-06-13）**

> 一度実装した（Terminal 1.0.7 の `CommandRecorded` イベント＋`Loomo.Core/Journal/`＋ジャーナルペイン＋
> OSC133 シェル統合注入）が、使用感がいまひとつで**同日撤回**。再挑戦する場合の主な知見：
> - PowerShell は既定で OSC133 を出さない。人間側の記録には **シェル統合プロンプトの注入が必須**
>   （prompt ラップで D/A/B、`PSConsoleHostReadLine` ラップで C。VS Code 流儀。動作検証済み）。
> - 注入はシェル統合前のレースでセンチネル（`__ASE_B_`）ラップが混入するため除外が要る。
> - UI 主導の自動 cd（フォルダ追従）の除外も必要。

**目的**: 可視ターミナルで人間が打った全コマンドを「時刻・cwd・コマンド・出力・exit code・所要時間」の
構造化レコードとして記録・検索・再利用できるようにする。§20 の AI トレースと対をなし、
§3「AIも人間も同じ経路で操作する」の人間側を観測可能にする。AI の実行
（`TerminalService.RunViaProcessAsync`、独立プロセス）も `source=agent` で同じジャーナルに合流させる。

**Terminal ライブラリ側の追加 API（`sk0ya.Terminal.Controls` 1.0.5 → 1.0.6）**
- 現状、OSC133 A/B/C/D は `AnsiTerminalBuffer.ShellCommandZoneReceived`（**internal**）が解釈し、
  `TerminalTabView.Agent.cs` が `RunCommandAsync`（エージェント実行中のみ）で消費するだけ。
  **人間が打ったコマンドの境界は外部に公開されていない** → public イベントを新設する：
  ```csharp
  // Terminal.Tabs.TerminalTabView に追加
  public event EventHandler<TerminalCommandRecord>? CommandRecorded;

  public sealed record TerminalCommandRecord(
      string Command,            // B→C 範囲のプレーンテキスト（プロンプト部除外）
      string Output,             // C→D 範囲（ANSI 除去済・ライブラリ側で上限カット 例64KB）
      int? ExitCode,             // OSC133 D の値
      DateTimeOffset StartedAt,  // C 受信時刻
      DateTimeOffset EndedAt);   // D 受信時刻
  ```
- 実装は `RunCommandAsync`（AGENT_API_SPEC §4-1）と同じゾーン抽出機構の**常時版**。
  スクロールバック破棄に備えゾーン受信時に逐次蓄積する。`RunCommandAsync` 経由の実行も同イベントに乗る。

**Loomo 側**
- `Loomo.Core/Journal/`（新規）:
  - `CommandJournalEntry`: ts / workspaceRoot / tabId / source(human|agent) / command / output(またはblob参照) /
    exitCode / durationMs / cwd(ベストエフォート)
  - `ICommandJournal`（Append / Query）＋ `JsonlCommandJournal`:
    `%APPDATA%/Loomo/journal/{yyyy-MM}.jsonl` へ追記専用。大出力は `journal/blobs/{sha256}` にスピル
    （§20.2 のトレースと同じ規約）。保持ポリシー（月数上限）も §20.7 に倣う。
- 配線: 各タブの view を握る `TerminalService` が `CommandRecorded` を購読して Append。
  AI 実行は既存 `CommandExecuted` イベントから `source=agent` で Append。
  cwd は OSC133 に乗らないため、可視ターミナルにも `TrackChdir` 相当の追跡を適用（ベストエフォート）。
- UI: **新 `PaneKind.Journal` ペイン**（`WorkspaceStateStore.PaneKind` に追加・既定 Hidden。
  追加手順は Trace/EditorSupport ペインの partial パターン＝`ShellWindow.PaneLayout.cs` の
  `_paneElements` 登録＋保存済みレイアウトへの後挿入 `InsertRelative`）。
  - 日毎グルーピングの仮想化リスト。1行 = 時刻・cwd（短縮）・コマンド・exit バッジ・所要時間。高密度（予約幅なし）。
  - フィルタ: テキスト / 失敗のみ / source(human|agent)。
  - 行アクション: **再実行**（可視ターミナルへ送出）/ **コンポーザへ**（§23.2）/
    **出力をエディタで開く**（`IEditorService.OpenDocumentAsync` の読み取り専用ドキュメント）/
    **ペグボードへ**（§23.3）/ コピー。
- フェーズ: **J-A** lib イベント＋JSONL 記録（UIなし・テストはレコード生成と JSONL 往復）→
  **J-B** ジャーナルペイン（閲覧・フィルタ）→ **J-C** cwd 追跡＋AI 合流＋行アクション連携。

### 23.2 コマンドコンポーザ（エディタ⇔ターミナルの織り合わせ）→ **実装完了（2026-06-13）**

> 実装メモ: 独立ペインではなく**ターミナルペインの下部にはめ込む**（同日のフィードバックで変更。
> ターミナルヘッダーのトグルボタンで表示/非表示、スプリッターで高さ調整、初表示で
> `VimEditorControl` を遅延生成）。「▶ 実行」または **Ctrl+Enter**（セクション内トンネリングで先取り）で
> 直上の可視ターミナルへ送信し、フォーカスはコンポーザに残す（推敲→再実行の往復のため）。
> 送信コマンドの組み立ては `ComposerCommandBuilder`（App/Services）：
> 単一行はそのまま、**複数行は常に `%APPDATA%/Loomo/composer-run.ps1` に書き出して `& '…'`**
> （下記案の「数行は `; ` 結合」は行コメント・行末パイプ・here-string を壊すため正確さ優先で不採用）。
> 本文は `WorkspaceSnapshot.ComposerText` としてワークスペース毎に保存・復元。
> シェル側は `ShellWindow.Composer.cs`。テスト: `ComposerPegboardTests`。

**目的**: 長い PowerShell をターミナルの1行ではなく **Vim エディタで組み立てて**、1アクションで
可視ターミナルへ送る。

- **形（推奨案）**: 専用 `PaneKind.Composer` ペインに `VimEditorControl` を 1 個埋め、
  フッターに「▶ ターミナルで実行（Ctrl+Enter）」。
  - `OpenDocumentAsync` の仮想ドキュメント（:w=送信）流用案は「保存=送信」の誤爆リスクがあるため不採用。
    専用ペインなら送信履歴・スニペット欄を将来足せる。
- **送信**: `TerminalTabView.RunCommandAsync`（既存 public API・画面にも通常表示される）。
  **複数行の扱いに注意**: そのまま `\r` 区切りで流すと PSReadLine が行単位に解釈する。
  既定は「数行以内は `; ` 結合／それ以上は一時 `.ps1` に書いて `& script.ps1`」の自動切替とし、挙動を設定で固定可能に。
- **逆方向**: 「直前の出力をエディタへ」はジャーナル行アクション（§23.1）が担う。コンポーザからの送信も
  OSC133 で普通にジャーナルに記録されるので、ジャーナル→コンポーザ→実行→ジャーナルが往復する。
- コンポーザ本文はワークスペース状態（`workspaces.json`）に保存（小さい文字列のみ）。

### 23.3 ペグボード（スクラップ作業台）→ **実装完了（2026-06-13）**

> 実装メモ: メインペインではなく**サイドバーパネル**（同日のフィードバックで変更。
> `SidebarPanel.Pegboard`＋ActivityBar の 📌 アイコン、`ShellViewModel.ShowPegboardCommand`）。
> `PegboardViewModel`/`PegboardView`（カード一覧・高密度・SessionsView と同じサイドバー流儀）。
> 追加経路は「＋ クリップボードから追加」（URL・実在絶対パスは `DetectType` で自動判別）と
> 「📌 ブラウザのURLをピン」（タイトル付き。URL 取得は `BrowserPinRequested` イベント経由で ShellWindow）。
> カード操作: コピー／開く（url→ブラウザ新タブ、file→エディタ・フォルダはターミナル cd、
> text→仮想ドキュメント）／削除／📌（上部固定・枠アクセント色）。
> 永続化は **`WorkspaceSnapshot.Pegboard`（workspaces.json 内）** — 下記案の専用ファイル分離は、
> エディタタブが既に全文を workspaces.json に保存している現状と揃えてインライン化（肥大化したら分離）。
> シェル側配線は `ShellWindow.Pegboard.cs`。テスト: `ComposerPegboardTests`。

**目的**: ターミナル出力片・スニペット・URL・ファイル参照を貼っておく作業台ペイン。§19.6（集合知）の非AI最小版。

- **新 `PaneKind.Pegboard` ペイン**。アイテム = `{ id, type: text|url|file, content, title?, createdAt, pinned? }`。
- 永続化: ワークスペース毎に `%APPDATA%/Loomo/pegboard/{workspaceKey}.json`（`workspaces.json` を肥大化させない）。
- 追加経路（v1）: 「クリップボードから追加」ボタン／エディタ・ターミナル選択の右クリック「ペグボードへ」／
  ジャーナル行の「ペグボードへ」／ブラウザ現在 URL のピン。ドラッグ&ドロップは v2。
- カード UI: 種別アイコン＋先頭数行プレビュー、クリック展開。アクション: コピー／エディタで開く／
  ブラウザで開く／削除。pinned を上部固定。高密度。

### 23.4 実装順序と横断事項

- 推奨順: **① ジャーナル J-A/J-B**（ライブラリ改修を先に流す）→ **② コンポーザ** → **③ ペグボード** →
  最後に行アクションの相互連携（J-C）をまとめて。
- 新ペイン3種はいずれも Trace ペインの追加パターンを踏襲（`PaneKind` 追加は `workspaces.json` 後方互換：
  旧ファイルにはリーフが無いだけで、EditorSupport と同じ「保存済みレイアウトへの Hidden 後挿入」で対応）。
- パッケージ更新は `src/Loomo.Services/Loomo.Services.csproj` の 1 箇所ピンのみ。Terminal 側の開発は
  `C:\Projects\Terminal`（nuget.org 公開のインデックス遅延・HTTP キャッシュに注意）。
- プライバシー: ジャーナルはコマンド・出力という機微データを含むため、§20.7 と同様に
  設定でオプトアウト（`EnableJournal`）と保持上限を設ける。

---

